defaults:
  - _self_
  - dataset: diver
  - model: stereo
  - hydra: default.yaml

image_size:
- 256
- 256

device: 0  # GPU ID
num_workers: 8  # Number of workers for data loading

data_path: ???
model_weight: ???
yolo_weight: ???

output_dir: result
